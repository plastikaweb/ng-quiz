import { ScoreStyleDirective } from './score-style.directive';

describe('ScoreStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new ScoreStyleDirective();
    expect(directive).toBeTruthy();
  });
});
